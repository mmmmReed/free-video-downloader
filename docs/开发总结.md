# 万能视频下载网站 - 开发总结

## 一、项目概述

**FreeVideo** 是一个在线万能视频下载工具，支持 YouTube、Bilibili、TikTok、抖音、Instagram 等 **1700+** 平台的视频解析与下载。用户只需粘贴视频链接，即可解析视频信息、选择清晰度并一键下载。

- **前端**：Vue 3 + TypeScript + Vite + Tailwind CSS 4
- **后端**：Python FastAPI + yt-dlp + 抖音专用解析模块
- **通信**：RESTful API + SSE (Server-Sent Events) 实时进度推送

## 二、已完成功能（第一期 MVP）

### 2.1 核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 视频链接解析 | ✅ 完成 | 支持 1700+ 平台，返回标题、封面、时长、可用格式列表 |
| 多格式下载 | ✅ 完成 | 支持选择不同清晰度（4K/1080p/720p 等）和格式（MP4/WebM/音频） |
| 实时下载进度 | ✅ 完成 | 通过 SSE 推送下载百分比、速度、预计剩余时间 |
| 文件下载 | ✅ 完成 | 下载完成后可保存到本地，自动清理临时文件 |
| 抖音专用解析 | ✅ 完成 | 独立模块绕过 yt-dlp 限制，支持无水印高清下载 |

### 2.2 前端页面

| 模块 | 文件 | 说明 |
|------|------|------|
| 顶部导航 | `Header.vue` | Logo + 导航菜单 + 登录按钮 |
| 主视觉区 | `HeroSection.vue` | 大标题 + URL 输入框 + 解析按钮，支持自动提取 URL |
| 视频结果 | `VideoResult.vue` | 封面、标题、格式选择、进度条、下载按钮 |
| 功能亮点 | `Features.vue` | 4 个功能卡片展示 |
| 平台展示 | `Platforms.vue` | 10 个主流平台标识 |
| 使用步骤 | `HowItWorks.vue` | 1-2-3 三步引导 |
| 定价方案 | `Pricing.vue` | 免费版 vs VIP 对比 |
| 页脚 | `Footer.vue` | 版权声明 + 免责说明 |

### 2.3 后端 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/parse` | POST | 解析视频信息，自动识别抖音链接走专用模块 |
| `/api/download` | POST | 启动下载任务，返回 task_id |
| `/api/progress/{task_id}` | GET (SSE) | 实时推送下载进度 |
| `/api/file/{task_id}` | GET | 下载已完成的文件 |
| `/api/file/{task_id}` | DELETE | 清理临时文件 |

## 三、关键技术决策与解决方案

### 3.1 抖音专用解析模块

**问题**：yt-dlp 解析抖音视频需要浏览器 Cookie 验证（反爬虫机制），且不支持 `/jingxuan?modal_id=xxx` 等非标准 URL 格式。

**方案**：为抖音单独实现 `services/douyin.py` 解析模块：

- **URL 规范化**：支持 `modal_id` 参数、`/video/xxx` 路径、`v.douyin.com` 短链接等多种格式
- **双重解析**：先尝试 `iesdouyin.com` 公开 API，失败后 fallback 到分享页 HTML 中的 `_ROUTER_DATA` 解析
- **WAF 挑战破解**：内置 SHA-256 验证挑战解决器
- **无水印下载**：将播放地址中的 `playwm` 替换为 `play` 获取无水印视频
- **路由分发**：`is_douyin_url()` 自动识别抖音链接，走专用通道；其他平台继续走 yt-dlp

参考开源实现：[rathodpratham-dev/douyin_video_downloader](https://github.com/rathodpratham-dev/douyin_video_downloader)（MIT License）

### 3.2 Bilibili 缩略图防盗链处理

**问题**：Bilibili 图片 CDN 通过 `Referer` 头防盗链，外部域名请求图片被 403 拒绝。

**方案**：在 `<img>` 标签添加 `referrerpolicy="no-referrer"`，让浏览器不发送 Referer 头。

### 3.3 视频格式智能合并

**问题**：许多平台的高清视频流是音视频分离的（视频流 + 音频流），用户不理解 `format_id` 的组合。

**方案**：`_build_combined_formats()` 自动将最佳音频流与各清晰度视频流组合，生成用户友好的格式选项（如 "1080p (mp4)"），后端使用 FFmpeg 自动合并。

### 3.4 错误提示可见性

**问题**：解析失败时错误提示在 `VideoResult` 组件内部，该组件受 `v-if="videoInfo"` 控制，解析失败时不渲染，用户看不到错误。

**方案**：在 `Home.vue` 中 `VideoResult` 之前增加独立的错误提示区域，确保解析失败时错误信息对用户可见。

## 四、项目结构

```
free-video-downloader/
├── docs/                        # 项目文档
│   ├── 需求分析.md
│   ├── 技术方案.md
│   ├── 竞品调研-AI视频总结.md
│   └── 开发总结.md
├── frontend/                    # Vue 3 前端
│   ├── src/
│   │   ├── App.vue              # 根组件
│   │   ├── main.ts              # 入口
│   │   ├── style.css            # Tailwind 主题配置
│   │   ├── views/Home.vue       # 主页面（集成下载 + AI 总结）
│   │   ├── components/          # 14 个 UI 组件
│   │   ├── composables/         # useVideoDownload + useVideoSummary
│   │   ├── api/                 # video.ts + summary.ts 请求封装
│   │   └── types/               # video.ts + summary.ts 类型定义
│   ├── vite.config.ts           # Vite 配置 + API 代理
│   └── package.json
├── backend/                     # FastAPI 后端
│   ├── main.py                  # FastAPI 入口 + CORS + dotenv
│   ├── requirements.txt         # Python 依赖
│   ├── .env                     # 环境变量（不提交 Git）
│   ├── routers/
│   │   ├── video.py             # 视频下载 API 路由
│   │   └── summary.py           # AI 总结 API 路由
│   ├── services/
│   │   ├── downloader.py        # yt-dlp 集成 + 任务管理
│   │   ├── douyin.py            # 抖音专用解析下载
│   │   ├── subtitle.py          # 字幕提取（B 站专用 + yt-dlp）
│   │   └── ai_summary.py        # DeepSeek API 总结与问答
│   └── downloads/               # 临时下载目录
└── README.md
```

## 五、测试验证

| 平台 | 解析 | 缩略图 | 格式选择 | 下载 | 进度显示 | 保存文件 |
|------|------|--------|----------|------|----------|----------|
| YouTube | ✅ | ✅ | ✅ 9 种格式 | ✅ | ✅ 实时 | ✅ |
| Bilibili | ✅ | ✅ (no-referrer) | ✅ | ✅ | ✅ | ✅ |
| 抖音 | ✅ (专用模块) | ✅ | ✅ 无水印 | ✅ | ✅ | ✅ |

## 六、第二期功能 —— AI 视频总结

### 6.1 功能概览

| 功能 | 优先级 | 状态 | 说明 |
|------|--------|------|------|
| 字幕提取 | P0 | ✅ 完成 | 自动提取视频字幕，支持多语言、带时间戳 |
| AI 一键总结 | P0 | ✅ 完成 | DeepSeek API 流式生成结构化 Markdown 总结 |
| 思维导图 | P1 | ✅ 完成 | 基于总结内容自动生成可交互思维导图 |
| AI 问答 | P1 | ✅ 完成 | 基于视频内容的多轮对话式提问 |

### 6.2 新增 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/subtitle` | POST | 提取视频字幕（B 站走专用 API，其他走 yt-dlp） |
| `/api/summarize` | POST (SSE) | 流式生成 AI 视频总结 |
| `/api/chat` | POST (SSE) | 基于视频内容的 AI 问答 |

### 6.3 新增前端组件

| 组件 | 说明 |
|------|------|
| `SummaryPanel.vue` | Tab 容器，管理 4 个子 Tab |
| `SummaryTab.vue` | AI 总结展示，实时 Markdown 渲染 |
| `SubtitleTab.vue` | 带时间戳的字幕列表 |
| `MindMapTab.vue` | markmap 思维导图渲染 |
| `ChatTab.vue` | AI 问答对话界面 |

### 6.4 关键技术决策

#### Bilibili 字幕专用提取

**问题**：yt-dlp 无法获取 B 站的 AI 生成字幕。B 站将字幕数据存放在独立的 `dm/view` API 中，yt-dlp 只能识别弹幕（danmaku）。

**方案**：为 B 站实现专用字幕提取链路：
1. 通过 `web-interface/view` API 获取视频的 `aid` 和 `cid`
2. 通过 `dm/view` API 获取字幕列表（含 AI 生成的中文/英文字幕 URL）
3. 按语言优先级选择最佳字幕，下载并解析为统一格式
4. 在 `extract_subtitle()` 入口优先走 B 站路径，失败时 fallback 到 yt-dlp

#### DeepSeek API 集成

- 使用 OpenAI Python SDK（`openai` 包），通过 `base_url` 指向 DeepSeek API
- 流式输出（`stream=True`），通过 SSE 实时推送给前端
- Prompt 设计要求输出固定 Markdown 结构：视频概述 → 章节摘要（带时间戳） → 核心知识点

#### 前端流式渲染

- AI 总结使用 `fetch` + `ReadableStream` 读取 SSE 流
- 使用 `marked` 库实时将 Markdown 渲染为 HTML
- 思维导图使用 `markmap-lib` + `markmap-view` 从 Markdown 自动生成

### 6.5 新增文件结构

```
backend/
├── services/
│   ├── subtitle.py          # 字幕提取（B 站专用 + yt-dlp 通用）
│   └── ai_summary.py        # DeepSeek API 总结与问答
├── routers/
│   └── summary.py           # 总结相关 API 路由
└── .env                     # DeepSeek API Key（不提交 Git）

frontend/src/
├── components/
│   ├── SummaryPanel.vue     # Tab 容器
│   ├── SummaryTab.vue       # AI 总结
│   ├── SubtitleTab.vue      # 字幕展示
│   ├── MindMapTab.vue       # 思维导图
│   └── ChatTab.vue          # AI 问答
├── composables/
│   └── useVideoSummary.ts   # 总结状态管理
├── api/
│   └── summary.ts           # API 封装
└── types/
    └── summary.ts           # 类型定义
```

### 6.6 测试验证

| 平台 | 字幕提取 | AI 总结 | 思维导图 | AI 问答 |
|------|----------|---------|----------|---------|
| YouTube | ✅ 人工字幕 60 段 | ✅ 流式 Markdown | ✅ | ✅ |
| Bilibili | ✅ AI 字幕 284 段 | ✅ 流式 Markdown | ✅ | ✅ |

### 6.7 依赖变更

**后端新增**：`openai>=1.0.0`、`python-dotenv>=1.0.0`

**前端新增**：`marked`、`markmap-lib`、`markmap-view`

## 七、后续规划（第三期）

1. **用户系统**：注册/登录/个人中心
2. **下载历史**：引入数据库持久化
3. **多语言翻译**：总结/字幕的多语言切换
4. **导出功能**：支持 Markdown/PDF 导出总结
5. **批量下载**：多链接同时解析下载
6. **付费系统**：支付宝/微信支付接入
7. **管理后台**：用户管理、数据统计
